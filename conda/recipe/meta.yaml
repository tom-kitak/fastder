package:
  name: fastder
  version: "0.0.1"

source:
  path: ../..   # points to the root of the repo

build:
  number: 0

requirements:
  build:
    - cmake >=4.0
    - make
    - gcc
    - gxx
    - pthread-stubs
  run:
    - libstdcxx-ng

about:
  home: "https://github.com/martina-lavanya/fastder"
  license: "MIT"
  summary: "fastder, a fast, agnostic, splice-junction-based tool for the detection of expressed regions in RNA-seq data"
